# Design Systems at Scale

Building a design system that actually gets used requires more than just components.

## Tokens First

Start with design tokens:

```css
:root {
    --color-primary: #2563eb;
    --spacing-unit: 0.25rem;
    --radius-base: 0.5rem;
}